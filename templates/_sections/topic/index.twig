{% extends "_layouts/main.twig" %}

{% block content %}

    {{ parent() }}

    {# Show articles related to topic #}
    {% include '_partials/entries-list.twig' with {
        paginate: true,
        layout: 'columns',
        query: craft.entries
            .section('article')
            .topics(entry)
            .orderBy('postDate desc')
    } only %}
{% endblock %}